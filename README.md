# Microsoft Windows Defender Demo

A local Microsoft Windows Defender-inspired scanner interface.

## Run locally

```powershell
npm install
npm run dev
```

The scanner is a visual simulation only. It does not access or scan local files.
